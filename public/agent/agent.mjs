#!/usr/bin/env node
/**
 * @deprecated Use agent v2: curl -fsSL .../agent/v2/install.sh | bash -s SOURCE_ID
 * This stub remains for old bookmarks only.
 */
console.error(
  "[agent v1] DEPRECATED. Use:\n  curl -fsSL https://leads.konversus.ru/agent/v2/install.sh | bash -s SOURCE_ID",
);
process.exit(1);
