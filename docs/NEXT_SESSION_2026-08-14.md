# План сессии · 14 августа 2026

> **Для нового чата с агентом.** Скопируй блок «Контекст для агента» в первое сообщение.

---

## Контекст для агента

```
Подключись к leads.konversus.ru по skill leads-konversus-connect.
Прочитай AGENTS.md, docs/PLAN_2026-08-10.md, последний блок DEVLOG.md.

Проект: хаб 109.196.165.106, PM2 leads-konversus :3005, profiOnHub: false.
Пилот: VPS 159.194.213.198, Profi RysyevIV (ниша сайты), TG 1600729589.
13.08: 13 заявок, CB CLOSED, agent v2 online. Часы 08:00–22:00 МСК.
TG-карточка v5 уже на хабе (бюджет/город/отклики). Пилот специально НЕ рестартили.

Запрещено: Playwright/Profi на хабе; pm2 start leads-profi; рестарт leads-agent-v2
без явной команды пользователя; deep scan; ускорять интервал проверки ленты;
сброс CB при login_failed; коммит .env.

Сегодня цель: наблюдение 4.6 + при зелёном утре — 5.8 (БД vs TG на пульте).
Не начинать 4.9 единый бот, пока 4.6 не закрыт.
```

---

## Утро (30–40 мин) — только смотреть

| # | Проверка | Ок если |
|---|---------|---------|
| 1 | `git pull` + `curl localhost:3005` + `pm2 list \| grep leads` | хаб 200, health online |
| 2 | Пульт `/dashboard/admin/ops` + доктор | не «зови агента» |
| 3 | Heartbeat пилота | <15 мин; CB `CLOSED` |
| 4 | Заявки за ночь 22:00–08:00 МСК | **0** — агент спал, это норма |
| 5 | Первая заявка после 08:00 | есть к ~10–12; TG ушёл партнёру |
| 6 | `login_failed` на пульте | только архив, не активная ошибка |

Если CB OPEN / BLOCKED / нет heartbeat в рабочие часы → **стоп, сообщить человеку**. Не рестартить вход.

---

## Приоритет 1 — дожать 4.6

- К вечеру 14.08: сутки без бана, заявки идут, CB не открывался.
- Объём сайтов: **8–15/день норма**. Живой поток после бэклога ~1/час.
- Если к **13:00 МСК** живых новых с утра **<3–4** — предложить расширить keywords (`тильда, tilda, wordpress, визитка`), **не** частоту скана.
- Не предлагать deep scan и не сокращать интервал 3–7 мин.

PLAN: 4.6 🟡 → ✅ только если 24ч чистые.

---

## Приоритет 2 — 5.8 (если утро зелёное)

Админ видит доставку в TG, не только записи в БД:

- Пульт: сегодня **в БД / в TG** по партнёру
- Вечерняя сводка админу: `имя: N в БД · M в TG` (+ ⚠ если M < N)
- Первая успешная доставка за день → одно сообщение админу (1×/партнёр/сутки)

Не слать админу копии самих заявок. Не рестартить агент.

---

## Не делать 14.08 (пока не скажут)

| | |
|---|---|
| Рестарт `leads-agent-v2` / пилота | без явной команды |
| Deep scan, stealth, другой интервал | риск бана |
| Этап 4.9 единый бот `/status` `/pause` | после 4.6 |
| Вертикаль `sites` vs `services` в коде | решение принято, кодить отдельно |
| Второй веб-студийный пилот | следующий живой клиент = мастер (ремонт) |
| 3.6 кнопки сброс CB / стоп | только если останется время и 4.6/5.8 закрыты |

---

## Продуктовые решения (уже согласованы)

- **Одна труба, две вертикали:** пилот = сайты; рынок = услуги (ремонт/стройка).
- Карточка v5 заточена под услуги; для сайтов позже — 1–2 строки сути, не простыня ТЗ.
- Больше заявок = шире слова / не резать «без бюджета», **не** чаще ходить на Profi.

---

## Команды быстрого старта

```bash
cd /var/www/www-root/data/www/leads.konversus.ru
git pull origin main
git status --short
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3005/
pm2 list | grep leads
npm run test:agent-core
```

Пульт: https://leads.konversus.ru/dashboard/admin/ops

---

## Риски

| Риск | Действие |
|------|----------|
| Тишина до 08:00 | не лечить — сон по часам |
| SMS/капча Profi | CB → стоп, партнёр у ПК, не авто-логин |
| Кажется «мало заявок» | ниша сайты; не крутить робота |
| Доктор «TG не отвечает» | проверить `.env` `TELEGRAM_BOT_TOKEN` **и** токен в settings |
